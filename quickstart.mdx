---
title: "Quickstart"
description: "Make your first BIZ MORI API call in minutes"
---

This guide walks you through creating an **Anti-AI protection order** — the most common integration pattern. The same flow applies to Watermark Embed orders.

## Prerequisites

- A BIZ MORI API key ([get one here](https://developers.mori.art))
- An image file to protect (`jpg`, `jpeg`, `png`, or `webp`)

## Step 1: Create an order

Choose your preferred input mode:

<Tabs>
  <Tab title="Upload Mode">
    Create an order and receive presigned S3 URLs for uploading your files.

    <CodeGroup>
    ```bash cURL
    curl -X POST https://morimori.app/api/v2/orders/anti-ai \
      -H "Authorization: Bearer YOUR_API_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{
        "idempotencyKey": "my-first-order-001",
        "files": [{ "fileName": "photo.jpg" }],
        "options": { "strength": "high" }
      }'
    ```

    ```javascript Node.js
    const response = await fetch('https://morimori.app/api/v2/orders/anti-ai', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer YOUR_API_TOKEN',
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        idempotencyKey: 'my-first-order-001',
        files: [{ fileName: 'photo.jpg' }],
        options: { strength: 'high' },
      }),
    });
    const { data } = await response.json();
    ```

    ```python Python
    import requests

    res = requests.post(
        'https://morimori.app/api/v2/orders/anti-ai',
        headers={'Authorization': 'Bearer YOUR_API_TOKEN'},
        json={
            'idempotencyKey': 'my-first-order-001',
            'files': [{'fileName': 'photo.jpg'}],
            'options': {'strength': 'high'},
        },
    )
    data = res.json()['data']
    ```

    ```php PHP
    <?php
    $ch = curl_init('https://morimori.app/api/v2/orders/anti-ai');
    curl_setopt_array($ch, [
        CURLOPT_POST           => true,
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_HTTPHEADER     => [
            'Authorization: Bearer YOUR_API_TOKEN',
            'Content-Type: application/json',
        ],
        CURLOPT_POSTFIELDS => json_encode([
            'idempotencyKey' => 'my-first-order-001',
            'files'          => [['fileName' => 'photo.jpg']],
            'options'        => ['strength' => 'high'],
        ]),
    ]);
    $data = json_decode(curl_exec($ch), true)['data'];
    curl_close($ch);
    ```

    ```ruby Ruby
    require 'net/http'
    require 'json'

    uri  = URI('https://morimori.app/api/v2/orders/anti-ai')
    http = Net::HTTP.new(uri.host, uri.port)
    http.use_ssl = true

    req = Net::HTTP::Post.new(uri)
    req['Authorization'] = 'Bearer YOUR_API_TOKEN'
    req['Content-Type']  = 'application/json'
    req.body = JSON.generate(
      idempotencyKey: 'my-first-order-001',
      files: [{ fileName: 'photo.jpg' }],
      options: { strength: 'high' }
    )

    data = JSON.parse(http.request(req).body)['data']
    ```

    ```go Go
    package main

    import (
    	"bytes"
    	"encoding/json"
    	"net/http"
    )

    body, _ := json.Marshal(map[string]any{
    	"idempotencyKey": "my-first-order-001",
    	"files":          []map[string]string{{"fileName": "photo.jpg"}},
    	"options":        map[string]string{"strength": "high"},
    })

    req, _ := http.NewRequest("POST",
    	"https://morimori.app/api/v2/orders/anti-ai",
    	bytes.NewBuffer(body),
    )
    req.Header.Set("Authorization", "Bearer YOUR_API_TOKEN")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    ```

    ```java Java
    import java.net.URI;
    import java.net.http.*;

    var body = "{\"idempotencyKey\":\"my-first-order-001\"," +
               "\"files\":[{\"fileName\":\"photo.jpg\"}]," +
               "\"options\":{\"strength\":\"high\"}}";

    HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("https://morimori.app/api/v2/orders/anti-ai"))
        .header("Authorization", "Bearer YOUR_API_TOKEN")
        .header("Content-Type", "application/json")
        .POST(HttpRequest.BodyPublishers.ofString(body))
        .build();

    var response = HttpClient.newHttpClient()
        .send(request, HttpResponse.BodyHandlers.ofString());
    ```
    </CodeGroup>

    **Response:**
    ```json
    {
      "data": {
        "orderName": "anti_ai_2026-02-19",
        "orderId": "123456789",
        "status": "pending",
        "files": [
          {
            "fileId": 1,
            "fileName": "photo.jpg",
            "uploadUrl": "https://s3.amazonaws.com/...",
            "fileKey": "temp/123456789/0/photo.jpg"
          }
        ]
      }
    }
    ```
  </Tab>
  <Tab title="URL Mode">
    Provide image URLs directly — processing starts immediately, no upload required.

    <CodeGroup>
    ```bash cURL
    curl -X POST https://morimori.app/api/v2/orders/anti-ai \
      -H "Authorization: Bearer YOUR_API_TOKEN" \
      -H "Content-Type: application/json" \
      -d '{
        "idempotencyKey": "my-first-order-001",
        "files": [
          {
            "fileName": "photo.jpg",
            "originalFileUrl": "https://your-server.com/photo.jpg"
          }
        ],
        "options": { "strength": "high" }
      }'
    ```

    ```javascript Node.js
    const response = await fetch('https://morimori.app/api/v2/orders/anti-ai', {
      method: 'POST',
      headers: {
        'Authorization': 'Bearer YOUR_API_TOKEN',
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({
        idempotencyKey: 'my-first-order-001',
        files: [{
          fileName: 'photo.jpg',
          originalFileUrl: 'https://your-server.com/photo.jpg',
        }],
        options: { strength: 'high' },
      }),
    });
    const { data } = await response.json();
    ```

    ```python Python
    import requests

    res = requests.post(
        'https://morimori.app/api/v2/orders/anti-ai',
        headers={'Authorization': 'Bearer YOUR_API_TOKEN'},
        json={
            'idempotencyKey': 'my-first-order-001',
            'files': [{
                'fileName': 'photo.jpg',
                'originalFileUrl': 'https://your-server.com/photo.jpg',
            }],
            'options': {'strength': 'high'},
        },
    )
    data = res.json()['data']
    ```

    ```php PHP
    <?php
    $ch = curl_init('https://morimori.app/api/v2/orders/anti-ai');
    curl_setopt_array($ch, [
        CURLOPT_POST           => true,
        CURLOPT_RETURNTRANSFER => true,
        CURLOPT_HTTPHEADER     => [
            'Authorization: Bearer YOUR_API_TOKEN',
            'Content-Type: application/json',
        ],
        CURLOPT_POSTFIELDS => json_encode([
            'idempotencyKey' => 'my-first-order-001',
            'files' => [[
                'fileName'        => 'photo.jpg',
                'originalFileUrl' => 'https://your-server.com/photo.jpg',
            ]],
            'options' => ['strength' => 'high'],
        ]),
    ]);
    $data = json_decode(curl_exec($ch), true)['data'];
    curl_close($ch);
    ```

    ```ruby Ruby
    require 'net/http'
    require 'json'

    uri  = URI('https://morimori.app/api/v2/orders/anti-ai')
    http = Net::HTTP.new(uri.host, uri.port)
    http.use_ssl = true

    req = Net::HTTP::Post.new(uri)
    req['Authorization'] = 'Bearer YOUR_API_TOKEN'
    req['Content-Type']  = 'application/json'
    req.body = JSON.generate(
      idempotencyKey: 'my-first-order-001',
      files: [{
        fileName: 'photo.jpg',
        originalFileUrl: 'https://your-server.com/photo.jpg'
      }],
      options: { strength: 'high' }
    )

    data = JSON.parse(http.request(req).body)['data']
    ```

    ```go Go
    body, _ := json.Marshal(map[string]any{
    	"idempotencyKey": "my-first-order-001",
    	"files": []map[string]string{{
    		"fileName":        "photo.jpg",
    		"originalFileUrl": "https://your-server.com/photo.jpg",
    	}},
    	"options": map[string]string{"strength": "high"},
    })

    req, _ := http.NewRequest("POST",
    	"https://morimori.app/api/v2/orders/anti-ai",
    	bytes.NewBuffer(body),
    )
    req.Header.Set("Authorization", "Bearer YOUR_API_TOKEN")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    ```

    ```java Java
    var body = "{\"idempotencyKey\":\"my-first-order-001\"," +
               "\"files\":[{\"fileName\":\"photo.jpg\"," +
               "\"originalFileUrl\":\"https://your-server.com/photo.jpg\"}]," +
               "\"options\":{\"strength\":\"high\"}}";

    HttpRequest request = HttpRequest.newBuilder()
        .uri(URI.create("https://morimori.app/api/v2/orders/anti-ai"))
        .header("Authorization", "Bearer YOUR_API_TOKEN")
        .header("Content-Type", "application/json")
        .POST(HttpRequest.BodyPublishers.ofString(body))
        .build();
    ```
    </CodeGroup>

    **Response:**
    ```json
    {
      "data": {
        "orderName": "anti_ai_2026-02-19",
        "orderId": "123456789",
        "status": "inProgress",
        "files": [
          {
            "fileId": 1,
            "fileName": "photo.jpg",
            "originalFileUrl": "https://your-server.com/photo.jpg"
          }
        ]
      }
    }
    ```

    <Info>
      With URL mode, skip to [Step 4](#step-4-check-order-status) — no upload or confirm step needed.
    </Info>
  </Tab>
</Tabs>

## Step 2: Upload files (Upload Mode only)

PUT your file to the presigned `uploadUrl` from Step 1. This is a direct S3 upload — **no Authorization header needed**.

```bash cURL
curl -X PUT "https://s3.amazonaws.com/..." \
  -H "Content-Type: image/jpeg" \
  --data-binary @photo.jpg
```

<Note>
  Presigned URLs expire after a set time. If yours has expired, use the [Refresh URLs](/api-reference/orders/refresh-urls) endpoint to generate new ones.
</Note>

## Step 3: Confirm the order (Upload Mode only)

After uploading all files, call confirm to start processing:

<CodeGroup>
```bash cURL
curl -X POST https://morimori.app/api/v2/orders/anti-ai/confirm \
  -H "Authorization: Bearer YOUR_API_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"idempotencyKey": "my-confirm-001", "orderId": "123456789"}'
```

```javascript Node.js
await fetch('https://morimori.app/api/v2/orders/anti-ai/confirm', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_TOKEN',
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    idempotencyKey: 'my-confirm-001',
    orderId: '123456789',
  }),
});
```

```python Python
requests.post(
    'https://morimori.app/api/v2/orders/anti-ai/confirm',
    headers={'Authorization': 'Bearer YOUR_API_TOKEN'},
    json={'idempotencyKey': 'my-confirm-001', 'orderId': '123456789'},
)
```

```php PHP
<?php
$ch = curl_init('https://morimori.app/api/v2/orders/anti-ai/confirm');
curl_setopt_array($ch, [
    CURLOPT_POST           => true,
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER     => [
        'Authorization: Bearer YOUR_API_TOKEN',
        'Content-Type: application/json',
    ],
    CURLOPT_POSTFIELDS => json_encode([
        'idempotencyKey' => 'my-confirm-001',
        'orderId'        => '123456789',
    ]),
]);
curl_exec($ch);
curl_close($ch);
```

```ruby Ruby
req = Net::HTTP::Post.new(URI('https://morimori.app/api/v2/orders/anti-ai/confirm'))
req['Authorization'] = 'Bearer YOUR_API_TOKEN'
req['Content-Type']  = 'application/json'
req.body = JSON.generate(idempotencyKey: 'my-confirm-001', orderId: '123456789')
http.request(req)
```

```go Go
body, _ := json.Marshal(map[string]string{
	"idempotencyKey": "my-confirm-001",
	"orderId":        "123456789",
})
req, _ := http.NewRequest("POST",
	"https://morimori.app/api/v2/orders/anti-ai/confirm",
	bytes.NewBuffer(body),
)
req.Header.Set("Authorization", "Bearer YOUR_API_TOKEN")
req.Header.Set("Content-Type", "application/json")
http.DefaultClient.Do(req)
```

```java Java
var body = "{\"idempotencyKey\":\"my-confirm-001\",\"orderId\":\"123456789\"}";
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://morimori.app/api/v2/orders/anti-ai/confirm"))
    .header("Authorization", "Bearer YOUR_API_TOKEN")
    .header("Content-Type", "application/json")
    .POST(HttpRequest.BodyPublishers.ofString(body))
    .build();
HttpClient.newHttpClient().send(request, HttpResponse.BodyHandlers.ofString());
```
</CodeGroup>

## Step 4: Check order status

Poll the order or use [webhooks](/webhooks) to receive a push notification when processing completes.

<CodeGroup>
```bash cURL
curl https://morimori.app/api/v2/orders/123456789 \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

```javascript Node.js
const res = await fetch('https://morimori.app/api/v2/orders/123456789', {
  headers: { 'Authorization': 'Bearer YOUR_API_TOKEN' },
});
const { data } = await res.json();
// data.status: 'pending' | 'inProgress' | 'complete' | 'failed'
```

```python Python
res = requests.get(
    'https://morimori.app/api/v2/orders/123456789',
    headers={'Authorization': 'Bearer YOUR_API_TOKEN'},
)
status = res.json()['data']['status']
```

```php PHP
<?php
$ch = curl_init('https://morimori.app/api/v2/orders/123456789');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER     => ['Authorization: Bearer YOUR_API_TOKEN'],
]);
$data = json_decode(curl_exec($ch), true)['data'];
curl_close($ch);
```

```ruby Ruby
req = Net::HTTP::Get.new(URI('https://morimori.app/api/v2/orders/123456789'))
req['Authorization'] = 'Bearer YOUR_API_TOKEN'
data = JSON.parse(http.request(req).body)['data']
```

```go Go
req, _ := http.NewRequest("GET",
	"https://morimori.app/api/v2/orders/123456789", nil)
req.Header.Set("Authorization", "Bearer YOUR_API_TOKEN")
resp, _ := http.DefaultClient.Do(req)
defer resp.Body.Close()
```

```java Java
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://morimori.app/api/v2/orders/123456789"))
    .header("Authorization", "Bearer YOUR_API_TOKEN")
    .GET()
    .build();
var response = HttpClient.newHttpClient()
    .send(request, HttpResponse.BodyHandlers.ofString());
```
</CodeGroup>

Order statuses:

| Status | Meaning |
|--------|---------|
| `pending` | Waiting for file upload |
| `inProgress` | Processing |
| `complete` | Ready for download |
| `failed` | Processing failed |

## Step 5: Download the result

Once `status` is `complete`, fetch the download URL:

<CodeGroup>
```bash cURL
curl https://morimori.app/api/v2/orders/123456789/download \
  -H "Authorization: Bearer YOUR_API_TOKEN"
```

```javascript Node.js
const res = await fetch('https://morimori.app/api/v2/orders/123456789/download', {
  headers: { 'Authorization': 'Bearer YOUR_API_TOKEN' },
});
const { data } = await res.json();
// data.downloadUrl — valid for 1 hour
```

```python Python
res = requests.get(
    'https://morimori.app/api/v2/orders/123456789/download',
    headers={'Authorization': 'Bearer YOUR_API_TOKEN'},
)
download_url = res.json()['data']['downloadUrl']
```

```php PHP
<?php
$ch = curl_init('https://morimori.app/api/v2/orders/123456789/download');
curl_setopt_array($ch, [
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_HTTPHEADER     => ['Authorization: Bearer YOUR_API_TOKEN'],
]);
$data = json_decode(curl_exec($ch), true)['data'];
curl_close($ch);
```

```ruby Ruby
req = Net::HTTP::Get.new(
  URI('https://morimori.app/api/v2/orders/123456789/download')
)
req['Authorization'] = 'Bearer YOUR_API_TOKEN'
data = JSON.parse(http.request(req).body)['data']
```

```go Go
req, _ := http.NewRequest("GET",
	"https://morimori.app/api/v2/orders/123456789/download", nil)
req.Header.Set("Authorization", "Bearer YOUR_API_TOKEN")
resp, _ := http.DefaultClient.Do(req)
defer resp.Body.Close()
```

```java Java
HttpRequest request = HttpRequest.newBuilder()
    .uri(URI.create("https://morimori.app/api/v2/orders/123456789/download"))
    .header("Authorization", "Bearer YOUR_API_TOKEN")
    .GET()
    .build();
var response = HttpClient.newHttpClient()
    .send(request, HttpResponse.BodyHandlers.ofString());
```
</CodeGroup>

**Response:**
```json
{
  "data": {
    "downloadUrl": "https://s3.amazonaws.com/...",
    "expiresIn": 3600
  }
}
```

The `downloadUrl` is a presigned S3 URL valid for **1 hour**. Download the protected file directly from this URL.

## Next steps

<Columns cols={2}>
  <Card title="Watermark Embed" icon="stamp" href="/api-reference/watermark-embed/create-order">
    Embed invisible watermarks into your images.
  </Card>
  <Card title="Webhooks" icon="bell" href="/webhooks">
    Set up webhooks to get notified when processing completes.
  </Card>
</Columns>
